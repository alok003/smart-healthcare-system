info:
  app:
    name: notification-service
    description: Notification Service for SHS.
    version: 1.0.0
server:
  port: 8015
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"