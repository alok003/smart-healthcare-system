spring:
  application:
    name: patient-service-test
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/test-patient
  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    enabled: false
